# CODEX_START_PROMPT.txt

Read `AGENTS.md`, `CURRENT_STATE.md`, and `CAPABILITY_MAP.md` first.

Then inspect the live repository before proposing or making changes.

Current context:

- Project: AI CAD ENGINEER.
- Branch: `cleanup/legacy-architecture`.
- Runtime architecture: external LLM → JSON → InventorCommandDispatcher → atomic command → support → Autodesk Inventor API.
- Runtime must remain only Eyes and atomic Hands.
- Do not restore EngineeringBrain, DrawingManager, Planning, Decision, or similar embedded reasoning systems.
- Commit and push are forbidden unless the user explicitly authorizes them for the current task.
- Never run reset, clean, checkout, or stash without explicit user instruction.

Current checkpoint context:

- Package 16 is closed.
- Package 16A added and verified typed Eyes for:
  - `get_parts_lists`
  - `get_revision_tables`
- Package 16C added and verified:
  - `get_drawing_table_collections`
- Inventor PASS confirmed that the visible GOST `Список деталей` table in `Sborka1.idw` is a `Sheet.CustomTables` / `kCustomTableObject`, not a `Sheet.PartsLists` object.

Next expected stage:

```text
Capability Audit: typed CustomTables Eye
```

Required next audit:

1. Run `git status --short --branch`.
2. Confirm the active branch and HEAD.
3. Read `InventorCommandDispatcher.cs`.
4. Search for existing commands that read:
   - `Sheet.CustomTables`
   - `CustomTable`
   - `Rows`
   - `Columns`
   - custom table cells.
5. Inspect existing table support:
   - `DrawingStructureReadSupport.cs`
   - `TableReadSupport.cs`
   - `GetDrawingTableCollectionsCommand.cs`
   - `GetPartsListsCommand.cs`
   - `GetRevisionTablesCommand.cs`
   - `GetDrawingTablesCommand.cs`
6. Determine whether a sufficient typed CustomTables Eye already exists.
7. If it exists, do not write code.
8. If it does not exist, report the exact gap and wait for approval before implementation unless the user explicitly authorized implementation in the same request.

Do not implement code during documentation-only tasks.

Do not update `CURRENT_STATE.md` or `CAPABILITY_MAP.md` until a capability has Build PASS and Inventor PASS.

Do not describe GOST `CustomTable` objects as `PartsList` objects.
