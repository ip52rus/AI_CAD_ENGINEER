# CODEX_START_PROMPT.txt

Read `AGENTS.md`, `CURRENT_STATE.md`, and `CAPABILITY_MAP.md` first.

Then inspect the live repository before proposing or making changes.

Current context:

- Project: AI CAD ENGINEER.
- Branch: `cleanup/legacy-architecture`.
- Runtime architecture: external LLM → JSON → InventorCommandDispatcher → atomic command → support → Autodesk Inventor API.
- Runtime must remain only Eyes and atomic Hands.
- Do not restore EngineeringBrain, DrawingManager, Planning, Decision, or similar embedded reasoning systems.
- Commit and push are forbidden unless the user explicitly authorizes them for the current task.
- Never run reset, clean, checkout, or stash without explicit user instruction.

Current checkpoint context:

- Package 20A is complete.
- Package 17A added and verified:
  - `get_custom_tables`
- Package 18A added and verified:
  - `get_drawing_text_objects`
- Package 19A added and verified:
  - `get_feature_control_frames`
- Package 20A added and verified:
  - `get_surface_texture_symbols`
- `get_custom_tables` reads Autodesk Inventor `Sheet.CustomTables` as a typed Eye:
  - CustomTable metadata;
  - Columns;
  - Rows;
  - Cells;
  - MergedCells;
  - reference keys;
  - diagnostics.
- CustomTables are not PartsLists.
- Runtime returns Inventor API facts only; it does not classify a table as specification, BOM, or GOST.
- `get_drawing_text_objects` reads DrawingNotes collections, DrawingSketch TextBoxes, and SketchedSymbols.
- `get_feature_control_frames` reads `Sheet.FeatureControlFrames`, frame metadata, rows, tolerance fields, datum fields, reference keys, and diagnostics.
- `get_surface_texture_symbols` reads `Sheet.SurfaceTextureSymbols`, SurfaceTextureSymbol metadata, roughness fields, production fields, sampling fields, definition data, reference keys, and diagnostics.
- Runtime does not perform semantic text analysis, TT/TU recognition, GOST interpretation, or engineering conclusions.
- Runtime does not perform tolerance interpretation, GOST validation, GD&T semantic analysis, or engineering conclusions.
- Runtime does not perform roughness interpretation, surface texture semantic analysis, GOST validation, or engineering conclusions.

Next expected stage:

```text
Capability Check - Welding Symbols Eye
```

Required next check:

1. Run `git status --short --branch`.
2. Confirm the active branch and HEAD.
3. Read `InventorCommandDispatcher.cs`.
4. Identify the next capability boundary from current user instructions.
5. Search for existing equivalent commands and support code.
6. Inspect actual Inventor 2027 interop signatures before proposing code.
7. If sufficient capability exists, do not write code.
8. If a gap exists, report the exact missing atomic Eye/Hand and wait for implementation approval unless the user explicitly authorizes implementation in the same request.

Do not implement code during documentation-only tasks.

Do not update `CURRENT_STATE.md` or `CAPABILITY_MAP.md` until a capability has Build PASS and Inventor PASS.
